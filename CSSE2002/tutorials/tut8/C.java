package tut8;

public class C extends A {
    /**
     * @require str!=null
     * @ensure (\result.length ==
     * (the minimum of str.length() and 10)) &&
     * && for each index i in \result,
     * \result[i] == str.charAt(i)
     */
    public char[] getChars(String str) {
        // ASSUME THAT A VALID IMPLEMENTATION IS HERE
        return null;
    }
}

//Answer: The precondition is weakened, and the precondition is strengthened.
